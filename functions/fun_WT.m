function dnoise= fun_WT(data)
%UNTITLED9 Summary of this function goes here
%   Detailed explanation goes here
x=length(data);
P1=zeros(x,1)*NaN;

        noise=data;
        wname = 'sym1';   %sym1
        lev = 5;  %5
        [c,l] = wavedec(noise,lev,wname);  
%????  
        sigma = wnoisest(c,l,1);%?????wnoisest???????????????????? 
        alpha=2;
        N = numel(noise);%???????  
        thr = sigma*sqrt(2*log(N));%????  
  
%??????  
        keepapp = 1;%????????  
        denoisexs = wdencmp('gbl',c,l,wname,lev,thr,'s',keepapp);  
%denoisexh = wdencmp('gbl',c,l,wname,lev,thr,'h',keepapp);  
        P1=denoisexs;

dnoise=data-P1;%??????????????????
end

